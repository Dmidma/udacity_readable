/*
import {
    ACTION_NAME
} from '../actions'
*/




export const reducerName = (state = {}, action) => {
    switch (action.type) {
        /*
        case ACTION_NAME:
            // operations
            return {
                // object that will be merged with the current
                // store state
            }
        */
        default:
            return state
    }
}
